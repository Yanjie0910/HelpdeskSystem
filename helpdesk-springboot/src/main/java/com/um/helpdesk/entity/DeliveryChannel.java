package com.um.helpdesk.entity;

public enum DeliveryChannel {
    EMAIL,              // Email notification
    SMS,                // SMS text message
    IN_APP,             // In-app notification (notification center)
    PUSH_NOTIFICATION   // Push notification (mobile/browser)
}
